name: 'Static Analysis'

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  static-analysis:
    name: Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      # see: https://github.com/AndanteTribe/Actions/tree/main/actions/checkout
      - uses: ./actions/checkout
        with:
          persist-credentials: 'false'
      # see: https://github.com/AndanteTribe/Actions/tree/main/actions/static-analysis
      - uses: ./actions/static-analysis
        with:
          token: ${{ github.token }}
